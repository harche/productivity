[project]
name = "trading"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "ib-async>=2.1.0",
    "ibkrbox>=0.6.0",
    "numpy<2",
    "pandas>=1.5.1,<2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
