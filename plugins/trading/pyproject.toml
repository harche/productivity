[project]
name = "trading"
version = "6.0.0"
description = "IB Client Portal REST API trading skill"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
